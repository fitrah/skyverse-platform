services:
  postgres:
    image: postgres:17-alpine
    container_name: skyverse-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: skyverse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - skyverse_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d skyverse"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  skyverse_postgres_data:
